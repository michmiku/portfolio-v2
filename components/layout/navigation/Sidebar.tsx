import { Navigtaion } from "utils/types/types";
import styles from "./styles.module.scss";

const Sidebar = ({ send, currentState }: Navigtaion) => {
  return <div className={styles.navigtaionSidebar}>Enter</div>;
};

export default Sidebar;
